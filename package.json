{
  "name": "okta-workspace-mcp",
  "version": "1.0.0",
  "description": "An Okta workspace MCP for identity context, assigned apps, groups, and read-only organization queries through OIDC and PKCE.",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "okta-workspace-mcp": "./build/cli.js"
  },
  "files": [
    "build",
    "scripts"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "init": "node build/cli.js init",
    "serve": "node build/cli.js serve",
    "watch": "tsc --watch",
    "smoke": "npm run build && node scripts/smoke-test.mjs",
    "collector": "node scripts/collector.mjs",
    "e2e:session-check": "node scripts/e2e-session-check.mjs",
    "post-cookie-proof": "node scripts/post-cookie-proof.mjs",
    "launch": "node scripts/launch.mjs",
    "inspector": "npx @modelcontextprotocol/inspector build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "open": "^10.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  },
  "mcpName": "local.okta/okta-workspace-mcp",
  "keywords": [
    "mcp",
    "okta",
    "identity",
    "sso",
    "iam",
    "ai",
    "security-research"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/oreneHack/okta_mcp.git"
  }
}
