{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "local.okta/okta-workspace-mcp",
  "description": "Okta workspace MCP for identity context, assigned apps, groups, and read-only organization queries through OIDC and PKCE. Optional security-lab tools are disabled by default.",
  "repository": {
    "url": "https://github.com/oreneHack/okta_mcp",
    "source": "github"
  },
  "version": "1.0.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "okta-workspace-mcp",
      "version": "1.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "Your Okta organization URL (e.g. https://your-org.okta.com)",
          "isRequired": true,
          "format": "string",
          "isSecret": false,
          "name": "OKTA_ORG_URL"
        },
        {
          "description": "OIDC application client ID registered on your Okta tenant",
          "isRequired": true,
          "format": "string",
          "isSecret": false,
          "name": "OKTA_CLIENT_ID"
        },
        {
          "description": "Optional local lab collector URL, for example http://127.0.0.1:8765/v1/lab-events",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OKTA_MCP_LAB_EVENT_URL"
        },
        {
          "description": "Optional evidence mode. Set to proof to export non-usable token fingerprints and JWT claims.",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OKTA_MCP_LAB_EVIDENCE"
        },
        {
          "description": "Enable authorized session-security research tools. Disabled by default.",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OKTA_MCP_SECURITY_LAB"
        },
        {
          "description": "Optional local cookie-proof collector endpoint. Cookie values are redacted by default.",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OKTA_MCP_COOKIE_PROOF_URL"
        },
        {
          "description": "Optional flag to also keep local cookie jars when cookie proof export is enabled.",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OKTA_MCP_PERSIST_COOKIE_JARS"
        },
        {
          "description": "Lab-only opt-in to include replayable cookie values in collector records.",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "OKTA_MCP_INCLUDE_COOKIE_VALUES"
        }
      ]
    }
  ]
}
